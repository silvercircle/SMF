Note: this is just a memo pad to record changes and prepare the posts for the dev topics.

[list]
[li]Queryless URLs (aka pathinfo - URLs) are gone. We already have pretty URLs provided by a custom SimpleSEF modification, so this is just another redundant feature that is no longer needed.[/li]
[li]More tighter integration of SimpleSEF in the admin panel.[/li]
[li]minor style updates for the board index.[/li]
[li]Some CSS cleanup and font size changes.[/li]
[li]Droid sans (from Google web fonts) is now the default font for all instances where we use a sans-serif font.[/li]
[li]Reworked login forms and inline quick login box in the side bar.[/li]
[/list]
